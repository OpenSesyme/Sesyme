<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".ViewImage">


    <ImageView
        android:id="@+id/full_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/error_loading_image" />

    <ImageView
        android:id="@+id/download_image"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentEnd="true"
        android:padding="20dp"
        android:src="@mipmap/ic_file_download"
        tools:ignore="ContentDescription" />

    <ProgressBar
        android:id="@+id/progress_view_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_centerInParent="true"/>

    <TextView
        android:id="@+id/progress_text_view_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="22sp"
        android:textColor="@color/iconsColor"
        android:layout_centerInParent="true"
        tools:ignore="RelativeOverlap" />
</RelativeLayout>